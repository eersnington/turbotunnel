---
"turbotunnel": patch
---

Add a terminal lifecycle UI for `tt http` and `tt dev` with readiness progress, reconnect notices, shutdown summaries, and clear boundaries between managed dev-server logs and tunnel status.
