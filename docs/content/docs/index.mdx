---
title: Get started
description: Tunnel your local dev server with a public URL, powered by Vercel WebSockets.
---

Turbotunnel forwards HTTP and WebSocket traffic from a public URL to an app running on your machine. The gateway runs in your Vercel account.

## Prerequisites

- Node.js 22 or newer
- A Vercel account
- The Vercel CLI, installed and logged in

```sh
npm i -g turbotunnel vercel
vercel login
```

## Deploy your gateway

Run this once:

```sh
tt deploy
```

Turbotunnel creates a Vercel project, deploys the gateway, verifies its status endpoint, and saves the connection settings in `~/.turbotunnel/config.json`.

## Open a tunnel

Start your local app, then pass its port to `tt http`:

```sh
tt http 5173
```

```text
. turbotunnel 0.0.2

  Public           https://ttdemo123-turbotunnel.vercel.app/
  Local            http://localhost:5173
  Relays           2 sockets

✓ Local app ready
✓ Tunnel ready in 1.2s

  Press Ctrl-C to stop
```

The public URL works until you press `Ctrl-C` or the process exits.

You can also let Turbotunnel start the package's `dev` script:

```sh
tt dev
```

`tt dev` prints the resolved tunnel and command before handing the terminal to the child process. It separates child logs from tunnel lifecycle messages so reconnects and readiness remain easy to find.

Run `tt status` to inspect tunnel processes on this machine. Run `tt list` to see recent tunnel presence reported by the configured gateway.

<CardGroup cols={2}>
  <Card title="Deploy command" href="/docs/deploy">
    Projects, domains, regions, and JSON output.
  </Card>
  <Card title="HTTP command" href="/docs/http">
    Slugs, local hosts, socket pools, and development overrides.
  </Card>
  <Card title="Dev command" href="/docs/dev">
    Managed dev servers, custom commands, ports, and child logs.
  </Card>
  <Card title="Status command" href="/docs/status">
    Local process health, relay state, and traffic counters.
  </Card>
  <Card title="List command" href="/docs/list">
    Gateway-wide tunnel presence and bounded consistency.
  </Card>
  <Card title="Configuration" href="/docs/configuration">
    Saved settings and environment variables.
  </Card>
  <Card title="How it works" href="/docs/how-it-works">
    Gateway, queue, and relay architecture.
  </Card>
</CardGroup>
